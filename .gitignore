node_modules/
.expo/
dist/
npm-debug.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.orig.*
web-build/
.env

# macOS
.DS_Store

# Temporary files created by Metro to check the health of the file watcher
.metro-health-check*

# Backend files (should not be bundled in React Native app)
# Note: Backend is deployed separately to Render
# These files are kept in git but excluded from Metro bundler
